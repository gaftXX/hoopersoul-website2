{%- style -%}
  #shopify-section-{{ section.id }} {
    margin: 0;
  }

  .vampire-landing-{{ section.id }} {
    position: relative;
    min-height: 100vh;
    overflow: hidden;
    background: #000;
    color: #fff;
    font-family: var(--font-heading-family);
    isolation: isolate;
  }

  .vampire-landing-{{ section.id }}__video-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
  }

  .vampire-landing-{{ section.id }}__video-bg-media {
    position: absolute;
    inset: 0;
    overflow: hidden;
  }

  .vampire-landing-{{ section.id }}__video-bg-media video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .vampire-landing-{{ section.id }}__video-bg-overlay {
    position: absolute;
    inset: 0;
    z-index: 1;
    background: #000;
    pointer-events: none;
  }

  .vampire-landing-{{ section.id }}__small-copy {
    position: absolute;
    top: 0;
    width: min(34rem, 42vw);
    font-size: clamp(0.68rem, 0.9vw, 0.95rem);
    line-height: 1.15;
    letter-spacing: 0.02em;
    text-transform: uppercase;
    opacity: 0.96;
    z-index: 3;
  }

  .vampire-landing-{{ section.id }}__small-copy--left {
    left: clamp(5px, 0.9vw, 10px);
    text-align: left;
  }

  .vampire-landing-{{ section.id }}__small-copy--right {
    right: clamp(5px, 0.9vw, 10px);
    text-align: right;
  }

  .vampire-landing-{{ section.id }}__wordmark {
    position: absolute;
    top: 14px;
    left: 50%;
    transform: translateX(-50%);
    margin: 0;
    width: max-content;
    filter: none;
    opacity: 1;
    z-index: 2;
    color: #fff;
  }

  .vampire-landing-{{ section.id }}__wordmark-logos {
    display: flex;
    align-items: center;
    gap: clamp(18px, 3vw, 44px);
  }

  .vampire-landing-{{ section.id }}__wordmark-logo-wrap {
    position: relative;
    width: clamp(72px, 6.8vw, 130px);
  }

  .vampire-landing-{{ section.id }}__wordmark-logo {
    display: block;
    width: 100%;
    height: auto;
    filter: blur(5.5px);
    transition: filter 220ms ease;
    transition-delay: 140ms;
    backface-visibility: hidden;
    transform: translateZ(0);
  }

  .vampire-landing-{{ section.id }}__wordmark-logo--base {
    opacity: 1;
    transition: opacity 140ms ease, filter 220ms ease;
    transition-delay: 0ms, 140ms;
  }

  .vampire-landing-{{ section.id }}__wordmark-logo--color {
    position: absolute;
    inset: 0;
    opacity: 0;
    transition: opacity 260ms ease;
    transition-delay: 0ms;
    pointer-events: none;
  }

  .vampire-landing-{{ section.id }}__wordmark:hover .vampire-landing-{{ section.id }}__wordmark-logo,
  .vampire-landing-{{ section.id }}__wordmark:focus-within .vampire-landing-{{ section.id }}__wordmark-logo {
    filter: none;
    transition-delay: 0ms;
  }

  .vampire-landing-{{ section.id }}__wordmark:hover .vampire-landing-{{ section.id }}__wordmark-logo--base,
  .vampire-landing-{{ section.id }}__wordmark:focus-within .vampire-landing-{{ section.id }}__wordmark-logo--base {
    opacity: 0;
  }

  .vampire-landing-{{ section.id }}__wordmark:hover .vampire-landing-{{ section.id }}__wordmark-logo-wrap--1 .vampire-landing-{{ section.id }}__wordmark-logo--base,
  .vampire-landing-{{ section.id }}__wordmark:focus-within .vampire-landing-{{ section.id }}__wordmark-logo-wrap--1 .vampire-landing-{{ section.id }}__wordmark-logo--base {
    transition-delay: 160ms, 0ms;
  }

  .vampire-landing-{{ section.id }}__wordmark:hover .vampire-landing-{{ section.id }}__wordmark-logo-wrap--1 .vampire-landing-{{ section.id }}__wordmark-logo--color,
  .vampire-landing-{{ section.id }}__wordmark:focus-within .vampire-landing-{{ section.id }}__wordmark-logo-wrap--1 .vampire-landing-{{ section.id }}__wordmark-logo--color {
    transition-delay: 160ms;
  }

  .vampire-landing-{{ section.id }}__wordmark:hover .vampire-landing-{{ section.id }}__wordmark-logo-wrap--2 .vampire-landing-{{ section.id }}__wordmark-logo--color,
  .vampire-landing-{{ section.id }}__wordmark:focus-within .vampire-landing-{{ section.id }}__wordmark-logo-wrap--2 .vampire-landing-{{ section.id }}__wordmark-logo--color {
    transition-delay: 230ms;
  }

  .vampire-landing-{{ section.id }}__wordmark:hover .vampire-landing-{{ section.id }}__wordmark-logo-wrap--2 .vampire-landing-{{ section.id }}__wordmark-logo--base,
  .vampire-landing-{{ section.id }}__wordmark:focus-within .vampire-landing-{{ section.id }}__wordmark-logo-wrap--2 .vampire-landing-{{ section.id }}__wordmark-logo--base {
    transition-delay: 230ms, 0ms;
  }

  .vampire-landing-{{ section.id }}__wordmark-mark {
    position: absolute;
    left: 100%;
    top: 4px;
    margin-left: 2px;
    font-size: clamp(11px, 1.1vw, 18px);
    font-weight: 300;
    line-height: 1;
    color: #fff;
    filter: none;
    user-select: none;
  }

  .vampire-landing-{{ section.id }}__wordmark:hover .vampire-landing-{{ section.id }}__wordmark-logo-wrap--3 .vampire-landing-{{ section.id }}__wordmark-logo--color,
  .vampire-landing-{{ section.id }}__wordmark:focus-within .vampire-landing-{{ section.id }}__wordmark-logo-wrap--3 .vampire-landing-{{ section.id }}__wordmark-logo--color {
    transition-delay: 300ms;
  }

  .vampire-landing-{{ section.id }}__wordmark:hover .vampire-landing-{{ section.id }}__wordmark-logo-wrap--3 .vampire-landing-{{ section.id }}__wordmark-logo--base,
  .vampire-landing-{{ section.id }}__wordmark:focus-within .vampire-landing-{{ section.id }}__wordmark-logo-wrap--3 .vampire-landing-{{ section.id }}__wordmark-logo--base {
    transition-delay: 300ms, 0ms;
  }

  .vampire-landing-{{ section.id }}__wordmark:focus-within .vampire-landing-{{ section.id }}__wordmark-mark {
    filter: none;
  }

  .vampire-landing-{{ section.id }}__wordmark:hover .vampire-landing-{{ section.id }}__wordmark-logo--color,
  .vampire-landing-{{ section.id }}__wordmark:focus-within .vampire-landing-{{ section.id }}__wordmark-logo--color {
    opacity: 1;
  }

  .vampire-landing-{{ section.id }}__cta {
    position: absolute;
    right: clamp(5px, 0.9vw, 12px);
    top: clamp(28px, 3.8vw, 50px);
    margin: 0;
    font-size: clamp(1.2rem, 2.7vw, 3.2rem);
    line-height: 0.9;
    text-transform: lowercase;
    z-index: 10002;
  }

  #shopify-section-{{ section.id }}.vampire-landing-about-drawer-open .vampire-landing-{{ section.id }}__cta {
    visibility: hidden;
    opacity: 0;
    pointer-events: none;
  }

  .vampire-landing-{{ section.id }}__center-copy {
    position: absolute;
    left: 50%;
    bottom: clamp(10px, 1.6vw, 20px);
    transform: translateX(-50%);
    margin: 0;
    width: min(92%, 1200px);
    font-size: clamp(3.2rem, 8.4vw, 8.8rem);
    line-height: 0.84;
    text-transform: uppercase;
    text-align: center;
    z-index: 3;
    letter-spacing: 0.01em;
  }

  .vampire-landing-{{ section.id }}__center-main {
    display: block;
    font-size: 1.16em;
    line-height: 0.82;
  }

  .vampire-landing-{{ section.id }}__tel {
    display: flex;
    justify-content: center;
    align-items: baseline;
    gap: 0.4em;
    font-family: inherit;
    text-transform: none;
    font-style: normal;
    font-weight: inherit;
    font-size: 0.46em;
    letter-spacing: -0.01em;
    margin-bottom: 0.04em;
  }

  .vampire-landing-{{ section.id }}__tel-meta {
    opacity: 0.55;
    font-size: 1em;
    font-weight: 400;
    letter-spacing: 0.01em;
  }

  .vampire-landing-{{ section.id }}__tel-link {
    color: inherit;
    text-decoration: none;
  }

  .vampire-landing-{{ section.id }}__tel-link:hover {
    text-decoration: underline;
    text-underline-offset: 0.08em;
  }

  .vampire-landing-{{ section.id }}__bottom-label {
    position: absolute;
    bottom: clamp(34px, 4.5vw, 64px);
    margin: 0;
    font-size: clamp(2.1rem, 5.7vw, 6.4rem);
    line-height: 0.9;
    text-transform: uppercase;
    /* Above corner copy / center so the link receives clicks and shows a pointer cursor */
    z-index: 6;
  }

  .vampire-landing-{{ section.id }}__bottom-label--left {
    left: clamp(5px, 0.9vw, 10px);
  }

  .vampire-landing-{{ section.id }}__bottom-label--right {
    right: clamp(5px, 0.9vw, 10px);
  }

  .vampire-landing-{{ section.id }}__bottom-label-link {
    color: inherit;
    text-decoration: none;
    display: inline-block;
    cursor: pointer;
    user-select: none;
    -webkit-user-select: none;
  }

  .vampire-landing-{{ section.id }}__bottom-label-link:hover {
    text-decoration: none;
  }

  .vampire-landing-{{ section.id }}__bottom-label-link:focus-visible {
    outline: 2px solid currentColor;
    outline-offset: 3px;
  }

  .vampire-landing-{{ section.id }}__corner-copy {
    position: absolute;
    bottom: clamp(3px, 0.6vw, 8px);
    width: min(26rem, 34vw);
    margin: 0;
    font-size: clamp(0.66rem, 0.86vw, 0.92rem);
    line-height: 1.12;
    letter-spacing: 0.02em;
    text-transform: uppercase;
    z-index: 3;
    opacity: 0.95;
  }

  .vampire-landing-{{ section.id }}__corner-copy--left {
    left: clamp(5px, 0.9vw, 10px);
    text-align: left;
  }

  .vampire-landing-{{ section.id }}__corner-copy--right {
    right: clamp(5px, 0.9vw, 10px);
    text-align: right;
  }

  @media screen and (max-width: 989px) {
    .vampire-landing-{{ section.id }} {
      min-height: 100svh;
    }

    .vampire-landing-{{ section.id }}__small-copy,
    .vampire-landing-{{ section.id }}__corner-copy {
      width: min(17rem, 47vw);
    }

    .vampire-landing-{{ section.id }}__center-copy {
      width: min(96%, 820px);
      bottom: clamp(10px, 1.6vw, 20px);
      font-size: clamp(2.7rem, 9.6vw, 6rem);
    }

    .vampire-landing-{{ section.id }}__contact-title,
    .vampire-landing-{{ section.id }}__about-title,
    .vampire-landing-{{ section.id }}__about-legal-title {
      font-size: calc(1.16 * clamp(2.7rem, 9.6vw, 6rem));
    }
  }

  @media screen and (max-width: 749px) {
    .vampire-landing-{{ section.id }}__small-copy,
    .vampire-landing-{{ section.id }}__corner-copy {
      display: none;
    }

    .vampire-landing-{{ section.id }}__wordmark {
      top: 48px;
    }

    .vampire-landing-{{ section.id }}__wordmark-logos {
      gap: 18px;
    }

    .vampire-landing-{{ section.id }}__wordmark-logo-wrap {
      width: clamp(54px, 22vw, 96px);
    }

    .vampire-landing-{{ section.id }}__wordmark-mark {
      font-size: clamp(9px, 3.8vw, 14px);
      top: 2px;
      margin-left: 2px;
    }

    .vampire-landing-{{ section.id }}__cta {
      top: 120px;
      right: 16px;
      font-size: clamp(1.25rem, 7vw, 2.3rem);
    }

    .vampire-landing-{{ section.id }}__center-copy {
      width: calc(100% - 24px);
      bottom: 20px;
      font-size: clamp(2.3rem, 10.2vw, 4.4rem);
    }

    .vampire-landing-{{ section.id }}__bottom-label {
      bottom: 32px;
      font-size: clamp(2.2rem, 12vw, 3.6rem);
    }
  }

  .vampire-landing-{{ section.id }}__cta-contact {
    margin: 0;
    padding: 0;
    border: none;
    background: none;
    font: inherit;
    color: inherit;
    text-transform: inherit;
    line-height: inherit;
    cursor: pointer;
    text-decoration: none;
  }

  .vampire-landing-{{ section.id }}__cta-contact:hover {
    text-decoration: none;
  }

  .vampire-landing-{{ section.id }}__cta-contact:focus-visible {
    text-decoration: none;
    outline: 2px solid currentColor;
    outline-offset: 3px;
  }

  .vampire-landing-{{ section.id }}__cta-rest {
    margin-inline-start: 0.35em;
  }

  .vampire-landing-{{ section.id }}__cta-about {
    margin-inline-start: 0.35em;
  }

  .vampire-landing-{{ section.id }}__contact-root {
    position: fixed;
    inset: 0;
    z-index: 9000;
    display: grid;
    grid-template-columns: 1fr 1fr;
    min-height: 0;
    height: 100vh;
    height: 100dvh;
    pointer-events: none;
    visibility: hidden;
  }

  .vampire-landing-{{ section.id }}__contact-root.is-open {
    pointer-events: auto;
    visibility: visible;
  }

  .vampire-landing-{{ section.id }}__contact-root:not(.is-open) .vampire-landing-{{ section.id }}__contact-panel,
  .vampire-landing-{{ section.id }}__contact-root:not(.is-open) .vampire-landing-{{ section.id }}__contact-scrim {
    pointer-events: none;
  }

  .vampire-landing-{{ section.id }}__contact-panel {
    grid-column: 1;
    grid-row: 1;
    background: #b400ff;
    color: #000;
    overflow: auto;
    padding: clamp(10px, 1.2vw, 18px) clamp(10px, 1.4vw, 22px) clamp(20px, 2.5vw, 36px);
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    box-sizing: border-box;
  }

  .vampire-landing-{{ section.id }}__contact-close {
    margin: 0 0 clamp(4px, 0.55vw, 9px);
    padding: 0;
    border: none;
    background: none;
    font-family: ui-monospace, 'Cascadia Code', 'Source Code Pro', Menlo, Consolas, monospace;
    font-size: clamp(1.35rem, 3.6vw, 2rem);
    font-weight: 300;
    letter-spacing: 0.02em;
    color: #000;
    cursor: pointer;
    text-transform: uppercase;
    text-decoration: none;
    position: relative;
    z-index: 2;
    user-select: none;
    -webkit-user-select: none;
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
  }

  .vampire-landing-{{ section.id }}__contact-close:hover {
    text-decoration: none;
  }

  .vampire-landing-{{ section.id }}__contact-close:focus-visible {
    text-decoration: none;
    outline: 2px solid currentColor;
    outline-offset: 3px;
  }

  .vampire-landing-{{ section.id }}__contact-title {
    margin: -0.08em 0 clamp(14px, 2.2vw, 28px);
    padding: 0;
    font-family: 'Proxima Nova', var(--font-heading-family), Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;
    font-weight: 900;
    font-size: calc(1.16 * clamp(3.2rem, 8.4vw, 8.8rem));
    line-height: 0.88;
    letter-spacing: -0.02em;
    text-transform: lowercase;
    color: #000;
    position: relative;
    z-index: 0;
    user-select: none;
    -webkit-user-select: none;
  }

  .vampire-landing-{{ section.id }}__contact-form-inner {
    width: 100%;
    max-width: 100%;
  }

  .vampire-landing-{{ section.id }}__contact-field {
    --vl-contact-field-inset: clamp(5px, 0.85vw, 8px);
    position: relative;
    margin-bottom: clamp(14px, 2vw, 22px);
    border: 1px solid #000;
    background: #b400ff;
    box-sizing: border-box;
  }

  .vampire-landing-{{ section.id }}__contact-input,
  .vampire-landing-{{ section.id }}__contact-textarea {
    display: block;
    width: 100%;
    margin: 0;
    border: none;
    background: transparent;
    font-family: ui-monospace, 'Cascadia Code', 'Source Code Pro', Menlo, Consolas, monospace;
    font-size: clamp(1.35rem, 3.6vw, 2rem);
    font-weight: 300;
    letter-spacing: 0.02em;
    line-height: 1.2;
    color: #000;
    box-sizing: border-box;
  }

  .vampire-landing-{{ section.id }}__contact-input {
    padding: var(--vl-contact-field-inset);
    height: calc(var(--vl-contact-field-inset) * 2 + 1.2em);
    overflow: hidden;
    -webkit-appearance: none;
    appearance: none;
    border-radius: 0;
    caret-color: #000;
  }

  .vampire-landing-{{ section.id }}__contact-textarea {
    padding: var(--vl-contact-field-inset);
    /* Five lines at line-height 1.2 → 6em text block */
    height: calc(var(--vl-contact-field-inset) * 2 + 6em);
    min-height: calc(var(--vl-contact-field-inset) * 2 + 6em);
    resize: vertical;
    -webkit-appearance: none;
    appearance: none;
    border-radius: 0;
    caret-color: #000;
    vertical-align: top;
  }

  .vampire-landing-{{ section.id }}__contact-input::placeholder,
  .vampire-landing-{{ section.id }}__contact-textarea::placeholder {
    font-family: inherit;
    font-size: inherit;
    font-weight: 300;
    font-style: normal;
    letter-spacing: inherit;
    line-height: 1.2;
    color: #000;
    opacity: 1;
  }

  .vampire-landing-{{ section.id }}__contact-input::-webkit-input-placeholder,
  .vampire-landing-{{ section.id }}__contact-textarea::-webkit-input-placeholder {
    font-family: inherit;
    font-size: inherit;
    font-weight: 300;
    line-height: 1.2;
    color: #000;
  }

  .vampire-landing-{{ section.id }}__contact-input::-moz-placeholder,
  .vampire-landing-{{ section.id }}__contact-textarea::-moz-placeholder {
    font-family: inherit;
    font-size: inherit;
    font-weight: 300;
    line-height: 1.2;
    color: #000;
    opacity: 1;
  }

  .vampire-landing-{{ section.id }}__contact-input:focus,
  .vampire-landing-{{ section.id }}__contact-input:focus-visible,
  .vampire-landing-{{ section.id }}__contact-textarea:focus,
  .vampire-landing-{{ section.id }}__contact-textarea:focus-visible {
    outline: none;
    box-shadow: none;
  }

  .vampire-landing-{{ section.id }}__contact-legal {
    margin: clamp(4px, 0.8vw, 10px) 0 clamp(14px, 1.85vw, 22px);
    font-family: ui-monospace, 'Cascadia Code', 'Source Code Pro', Menlo, Consolas, monospace;
    font-size: clamp(1.35rem, 3.6vw, 2rem);
    font-weight: 400;
    letter-spacing: 0.02em;
    line-height: 1.35;
  }

  .vampire-landing-{{ section.id }}__contact-legal-label {
    display: flex;
    align-items: center;
    gap: 0.55em;
    cursor: pointer;
    color: #000;
  }

  .vampire-landing-{{ section.id }}__contact-legal-copy {
    display: inline-flex;
    flex-wrap: wrap;
    align-items: baseline;
    column-gap: 0.45em;
    row-gap: 0.2em;
    line-height: 1.35;
  }

  .vampire-landing-{{ section.id }}__contact-legal-preface {
    white-space: nowrap;
  }

  .vampire-landing-{{ section.id }}__contact-checkbox {
    flex-shrink: 0;
    width: 1.45em;
    height: 1.45em;
    min-width: 1.45em;
    min-height: 1.45em;
    aspect-ratio: 1;
    margin: 0;
    cursor: pointer;
    align-self: center;
    -webkit-appearance: none;
    -moz-appearance: none;
    appearance: none;
    border: 1px solid #000;
    border-radius: 50%;
    background: #b400ff;
    box-sizing: border-box;
  }

  .vampire-landing-{{ section.id }}__contact-checkbox:checked {
    background: #000;
    border-color: #000;
  }

  .vampire-landing-{{ section.id }}__contact-checkbox:focus-visible {
    outline: 1px solid #000;
    outline-offset: 3px;
  }

  .vampire-landing-{{ section.id }}__contact-legal-link {
    color: inherit;
    text-decoration: underline;
    text-underline-offset: 0.12em;
  }

  button.vampire-landing-{{ section.id }}__contact-legal-link {
    font: inherit;
    font-size: inherit;
    font-weight: inherit;
    font-family: inherit;
    letter-spacing: inherit;
    line-height: inherit;
    padding: 0;
    margin: 0;
    border: none;
    background: none;
    cursor: pointer;
    display: inline;
    text-align: inherit;
    color: inherit;
  }

  .vampire-landing-{{ section.id }}__contact-send {
    margin: clamp(-7px, -0.65vw, -2px) 0 0;
    padding: 0;
    border: none;
    background: none;
    font-family: 'Proxima Nova', var(--font-heading-family), Impact, Haettenschweiler, sans-serif;
    font-weight: 900;
    font-size: clamp(2.85rem, 7vw, 5rem);
    letter-spacing: 0.02em;
    text-transform: uppercase;
    color: #000;
    cursor: pointer;
    line-height: 1;
  }

  .vampire-landing-{{ section.id }}__contact-send:hover,
  .vampire-landing-{{ section.id }}__contact-send:focus-visible {
    text-decoration: none;
  }

  .vampire-landing-{{ section.id }}__contact-scrim {
    grid-column: 2;
    grid-row: 1;
    min-height: 0;
    border: none;
    padding: 0;
    margin: 0;
    cursor: pointer;
    background: transparent;
  }

  .vampire-landing-{{ section.id }}__about-root {
    position: fixed;
    inset: 0;
    z-index: 9010;
    display: grid;
    grid-template-columns: 1fr 1fr;
    min-height: 0;
    height: 100vh;
    height: 100dvh;
    pointer-events: none;
    visibility: hidden;
    overflow: hidden;
  }

  .vampire-landing-{{ section.id }}__about-root.is-open {
    pointer-events: auto;
    visibility: visible;
  }

  .vampire-landing-{{ section.id }}__about-root:not(.is-open) .vampire-landing-{{ section.id }}__about-panel,
  .vampire-landing-{{ section.id }}__about-root:not(.is-open) .vampire-landing-{{ section.id }}__about-scrim {
    pointer-events: none;
  }

  .vampire-landing-{{ section.id }}__about-scrim {
    grid-column: 1;
    grid-row: 1;
    min-height: 0;
    border: none;
    padding: 0;
    margin: 0;
    cursor: pointer;
    background: transparent;
  }

  .vampire-landing-{{ section.id }}__about-panel {
    position: relative;
    grid-column: 2;
    grid-row: 1;
    height: 100%;
    min-height: 0;
    background: #0500ff;
    color: #fff;
    overflow: hidden;
    padding: clamp(10px, 1.2vw, 18px) clamp(10px, 1.4vw, 22px) clamp(20px, 2.5vw, 36px);
    display: grid;
    grid-template-rows: auto minmax(0, 1fr) auto;
    align-content: start;
    box-sizing: border-box;
    isolation: isolate;
  }

  .vampire-landing-{{ section.id }}__about-header {
    grid-row: 1;
    position: relative;
    z-index: 3;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    justify-content: flex-start;
    gap: clamp(2px, 0.45vw, 8px);
    width: 100%;
    box-sizing: border-box;
    background: #0500ff;
    pointer-events: auto;
  }

  .vampire-landing-{{ section.id }}__about-mid {
    grid-row: 2;
    position: relative;
    z-index: 0;
    min-height: 0;
    min-width: 0;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    align-self: stretch;
    box-sizing: border-box;
    padding-left: clamp(16px, 3.8vw, 56px);
    padding-top: clamp(10px, 2vw, 22px);
    overflow-x: hidden;
    overflow-y: auto;
    -webkit-overflow-scrolling: touch;
    overscroll-behavior: contain;
  }

  .vampire-landing-{{ section.id }}__about-copy {
    margin: 0;
    padding: 0;
    width: min(56rem, 100%);
    max-width: 100%;
    align-self: flex-end;
    box-sizing: border-box;
    border: none;
    background: none;
    box-shadow: none;
    font-family: ui-monospace, 'Cascadia Code', 'Source Code Pro', Menlo, Consolas, monospace;
    font-size: clamp(1.35rem, 3.6vw, 2rem);
    font-weight: 300;
    line-height: 1.35;
    letter-spacing: 0.02em;
    color: #fff;
    direction: ltr;
    text-align: justify;
    text-align-last: left;
    text-justify: inter-word;
    hyphens: auto;
    -webkit-hyphens: auto;
    -ms-hyphens: auto;
    overflow-wrap: break-word;
    outline: none;
  }

  .vampire-landing-{{ section.id }}__about-copy p {
    margin: 0;
    text-align: justify;
    text-align-last: left;
  }

  .vampire-landing-{{ section.id }}__about-close {
    margin: 0;
    flex-shrink: 0;
    padding: 0;
    border: none;
    background: none;
    font-family: ui-monospace, 'Cascadia Code', 'Source Code Pro', Menlo, Consolas, monospace;
    font-size: clamp(1.35rem, 3.6vw, 2rem);
    font-weight: 300;
    letter-spacing: 0.02em;
    color: #fff;
    cursor: pointer !important;
    text-transform: uppercase;
    text-decoration: none;
    position: relative;
    z-index: 4;
    user-select: none;
    -webkit-user-select: none;
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
  }

  .vampire-landing-{{ section.id }}__about-close:hover {
    text-decoration: none;
  }

  .vampire-landing-{{ section.id }}__about-close:focus-visible {
    text-decoration: none;
    outline: 2px solid currentColor;
    outline-offset: 3px;
  }

  .vampire-landing-{{ section.id }}__about-title,
  .vampire-landing-{{ section.id }}__about-legal-title {
    padding: 0;
    font-family: 'Proxima Nova', var(--font-heading-family), Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;
    font-weight: 900;
    font-size: calc(1.16 * clamp(3.2rem, 8.4vw, 8.8rem));
    line-height: 0.88;
    letter-spacing: -0.02em;
    text-transform: lowercase;
    color: #fff;
  }

  .vampire-landing-{{ section.id }}__about-title {
    margin: -0.08em 0 0;
  }

  .vampire-landing-{{ section.id }}__about-footer {
    grid-row: 3;
    position: relative;
    z-index: 2;
    width: 100%;
    box-sizing: border-box;
    display: flex;
    justify-content: flex-end;
    padding-top: clamp(14px, 2.5vw, 28px);
    background: #0500ff;
    pointer-events: auto;
  }

  .vampire-landing-{{ section.id }}__about-legal-title {
    margin: 0;
    cursor: default;
    display: block;
    user-select: none;
    text-wrap: balance;
  }

  .vampire-landing-{{ section.id }}__contact-status {
    font-family: ui-monospace, 'Cascadia Code', 'Source Code Pro', Menlo, Consolas, monospace;
    font-size: clamp(0.75rem, 1.05vw, 0.9rem);
    line-height: 1.45;
    margin: 0 0 1rem;
  }

  @media screen and (max-width: 749px) {
    .vampire-landing-{{ section.id }}__contact-title,
    .vampire-landing-{{ section.id }}__about-title,
    .vampire-landing-{{ section.id }}__about-legal-title {
      font-size: calc(1.16 * clamp(2.3rem, 10.2vw, 4.4rem));
    }

    .vampire-landing-{{ section.id }}__contact-send {
      font-size: clamp(2.35rem, 11vw, 3.75rem);
    }
  }
{%- endstyle -%}

<section class="vampire-landing-{{ section.id }}" aria-label="{{ section.settings.aria_label | escape }}">
  {%- liquid
    assign bg_shopify = section.settings.background_video
    assign bg_asset = section.settings.background_video_asset | strip
    assign bg_visible = false
    if bg_shopify != blank
      assign bg_visible = true
    elsif bg_asset != blank
      assign bg_visible = true
    endif
  -%}
  {%- if bg_visible -%}
    <div class="vampire-landing-{{ section.id }}__video-bg" aria-hidden="true">
      <div class="vampire-landing-{{ section.id }}__video-bg-media">
        {%- if bg_shopify != blank -%}
          {%- capture bg_video_markup -%}
            {{
              bg_shopify
              | video_tag:
                image_size: '3840x',
                autoplay: true,
                loop: true,
                muted: true,
                controls: false
            }}
          {%- endcapture -%}
          {{
            bg_video_markup
            | replace_first:
              '<video',
              '<video playsinline="" data-pause-all-media-exempt=""'
          }}
        {%- elsif bg_asset != blank -%}
          {%- assign bg_ext = bg_asset | split: '.' | last | downcase -%}
          {%- liquid
            assign bg_mime = 'video/mp4'
            if bg_ext == 'webm'
              assign bg_mime = 'video/webm'
            elsif bg_ext == 'mov' or bg_ext == 'qt'
              assign bg_mime = 'video/quicktime'
            endif
          -%}
          <video
            class="vampire-landing-{{ section.id }}__video-bg-el"
            autoplay
            muted
            playsinline
            loop
            data-pause-all-media-exempt
            preload="metadata"
          >
            <source src="{{ bg_asset | asset_url }}" type="{{ bg_mime }}">
          </video>
        {%- endif -%}
      </div>
      {%- if section.settings.background_overlay_opacity > 0 -%}
        <div
          class="vampire-landing-{{ section.id }}__video-bg-overlay"
          style="opacity: {{ section.settings.background_overlay_opacity | divided_by: 100.0 }};"
        ></div>
      {%- endif -%}
    </div>
  {%- endif -%}

  <p class="vampire-landing-{{ section.id }}__small-copy vampire-landing-{{ section.id }}__small-copy--left">
    {{ section.settings.small_copy }}
  </p>
  <p class="vampire-landing-{{ section.id }}__small-copy vampire-landing-{{ section.id }}__small-copy--right">
    {{ section.settings.small_copy }}
  </p>

  <h1 class="vampire-landing-{{ section.id }}__wordmark">
    <span class="vampire-landing-{{ section.id }}__wordmark-logos">
      <span class="vampire-landing-{{ section.id }}__wordmark-logo-wrap vampire-landing-{{ section.id }}__wordmark-logo-wrap--1">
        <img
          src="{{ 'logo_HS.svg' | asset_url }}"
          alt="{{ section.settings.brand_wordmark | escape }}"
          class="vampire-landing-{{ section.id }}__wordmark-logo vampire-landing-{{ section.id }}__wordmark-logo--base"
          width="590"
          height="706"
          loading="eager"
        >
        <img
          src="{{ 'logo_HS_b400ff.svg' | asset_url }}"
          alt=""
          aria-hidden="true"
          class="vampire-landing-{{ section.id }}__wordmark-logo vampire-landing-{{ section.id }}__wordmark-logo--color"
          width="590"
          height="706"
          loading="eager"
        >
      </span>
      <span class="vampire-landing-{{ section.id }}__wordmark-logo-wrap vampire-landing-{{ section.id }}__wordmark-logo-wrap--2">
        <img
          src="{{ 'logo_HS.svg' | asset_url }}"
          alt=""
          aria-hidden="true"
          class="vampire-landing-{{ section.id }}__wordmark-logo vampire-landing-{{ section.id }}__wordmark-logo--base"
          width="590"
          height="706"
          loading="eager"
        >
        <img
          src="{{ 'logo_HS_0500ff.svg' | asset_url }}"
          alt=""
          aria-hidden="true"
          class="vampire-landing-{{ section.id }}__wordmark-logo vampire-landing-{{ section.id }}__wordmark-logo--color"
          width="590"
          height="706"
          loading="eager"
        >
      </span>
      <span class="vampire-landing-{{ section.id }}__wordmark-logo-wrap vampire-landing-{{ section.id }}__wordmark-logo-wrap--3">
        <img
          src="{{ 'logo_HS.svg' | asset_url }}"
          alt=""
          aria-hidden="true"
          class="vampire-landing-{{ section.id }}__wordmark-logo vampire-landing-{{ section.id }}__wordmark-logo--base"
          width="590"
          height="706"
          loading="eager"
        >
        <img
          src="{{ 'logo_HS_05ff00.svg' | asset_url }}"
          alt=""
          aria-hidden="true"
          class="vampire-landing-{{ section.id }}__wordmark-logo vampire-landing-{{ section.id }}__wordmark-logo--color"
          width="590"
          height="706"
          loading="eager"
        >
      </span>
    </span>
    <span class="vampire-landing-{{ section.id }}__wordmark-mark" aria-hidden="true">&reg;</span>
  </h1>
  {%- liquid
    assign cta_words = section.settings.top_right_label | split: ' '
    assign cta_first = cta_words | first
    assign cta_second = ''
    assign cta_tail_joined = ''
    if cta_words.size > 1
      for w in cta_words offset: 1
        if forloop.first
          assign cta_second = w
        else
          if cta_tail_joined == ''
            assign cta_tail_joined = w
          else
            assign cta_tail_joined = cta_tail_joined | append: ' ' | append: w
          endif
        endif
      endfor
    endif
    assign vampire_form_id = 'ContactForm-vampire-' | append: section.id
    assign vampire_form_class = 'vampire-landing-' | append: section.id | append: '__contact-form-inner'
    assign vampire_terms_url = shop.terms_of_service.url
    if vampire_terms_url == blank and section.settings.legal_terms_url != blank
      assign vampire_terms_url = section.settings.legal_terms_url
    endif
  -%}
  <p class="vampire-landing-{{ section.id }}__cta">
    <button
      type="button"
      class="vampire-landing-{{ section.id }}__cta-contact"
      id="vampire-contact-trigger-{{ section.id }}"
      aria-expanded="false"
      aria-controls="vampire-contact-dialog-{{ section.id }}"
    >
      {{- cta_first | default: 'contact' -}}
    </button>
    {%- if cta_second != '' -%}
      <button
        type="button"
        class="vampire-landing-{{ section.id }}__cta-contact vampire-landing-{{ section.id }}__cta-about"
        id="vampire-about-trigger-{{ section.id }}"
        aria-expanded="false"
        aria-controls="vampire-about-dialog-{{ section.id }}"
      >
        {{- cta_second -}}
      </button>
    {%- endif -%}
    {%- if cta_tail_joined != '' -%}
      <span class="vampire-landing-{{ section.id }}__cta-rest">
        {{ cta_tail_joined }}
      </span>
    {%- endif -%}
  </p>

  <p class="vampire-landing-{{ section.id }}__center-copy">
    <span class="vampire-landing-{{ section.id }}__tel">
      {{ section.settings.center_text }}
      <span class="vampire-landing-{{ section.id }}__tel-meta">22 MAY 10AM GMT+2</span>
    </span>
    <span class="vampire-landing-{{ section.id }}__center-main" id="vampire-countdown-{{ section.id }}">
      {{ section.settings.phone_line }}
    </span>
  </p>

  {%- liquid
    assign vampire_shop_href = section.settings.shop_page_url
    if vampire_shop_href == blank and pages.shop != blank
      assign vampire_shop_href = pages.shop.url
    endif
    if vampire_shop_href == blank
      assign vampire_shop_href = '/pages/shop'
    endif
  -%}
  <a
    href="{{ vampire_shop_href }}"
    class="vampire-landing-{{ section.id }}__bottom-label vampire-landing-{{ section.id }}__bottom-label--left vampire-landing-{{ section.id }}__bottom-label-link"
  >
    {{- section.settings.left_bottom_label | escape -}}
  </a>
  <a
    href="{{ vampire_shop_href }}"
    class="vampire-landing-{{ section.id }}__bottom-label vampire-landing-{{ section.id }}__bottom-label--right vampire-landing-{{ section.id }}__bottom-label-link"
  >
    {{- section.settings.right_bottom_label | escape -}}
  </a>

  <p class="vampire-landing-{{ section.id }}__corner-copy vampire-landing-{{ section.id }}__corner-copy--left">
    {{ section.settings.small_copy }}
  </p>
  <p class="vampire-landing-{{ section.id }}__corner-copy vampire-landing-{{ section.id }}__corner-copy--right">
    {{ section.settings.small_copy }}
  </p>

  <div
    id="vampire-contact-dialog-{{ section.id }}"
    class="vampire-landing-{{ section.id }}__contact-root"
    role="dialog"
    aria-modal="true"
    aria-hidden="true"
    aria-labelledby="vampire-contact-title-{{ section.id }}"
  >
    <aside class="vampire-landing-{{ section.id }}__contact-panel">
      <button
        type="button"
        class="vampire-landing-{{ section.id }}__contact-close"
        data-vampire-close-contact
      >
        [CLOSE]
      </button>
      <h2 class="vampire-landing-{{ section.id }}__contact-title" id="vampire-contact-title-{{ section.id }}">contact</h2>

      {%- form 'contact', id: vampire_form_id, class: vampire_form_class -%}
        <div
          id="vampire-contact-success-{{ section.id }}"
          class="vampire-landing-{{ section.id }}__contact-status"
          {% unless form.posted_successfully? %}
            hidden
          {% endunless %}
          tabindex="-1"
        >
          {{ 'templates.contact.form.post_success' | t }}
        </div>

        {%- if form.errors -%}
          <div class="vampire-landing-{{ section.id }}__contact-status" role="alert" tabindex="-1">
            {{ 'templates.contact.form.error_heading' | t }}
            {%- if form.errors contains 'email' -%}
              — {{ form.errors.messages.email }}
            {%- endif -%}
          </div>
        {%- endif -%}

        <div
          id="vampire-contact-fields-{{ section.id }}"
          {% if form.posted_successfully? %}
            hidden
          {% endif %}
        >
          <div class="vampire-landing-{{ section.id }}__contact-field">
            <label class="visually-hidden" for="VampireContact-name-{{ section.id }}">
              {{- 'templates.contact.form.name' | t -}}
            </label>
            <input
              type="text"
              class="vampire-landing-{{ section.id }}__contact-input"
              id="VampireContact-name-{{ section.id }}"
              name="contact[{{ 'templates.contact.form.name' | t }}]"
              autocomplete="name"
              placeholder="{{ 'templates.contact.form.name' | t }}*"
              value="{% if form.name %}{{ form.name }}{% elsif customer %}{{ customer.name }}{% endif %}"
              required
            >
          </div>

          <div class="vampire-landing-{{ section.id }}__contact-field">
            <label class="visually-hidden" for="VampireContact-email-{{ section.id }}">
              {{- 'templates.contact.form.email' | t -}}
            </label>
            <input
              type="email"
              class="vampire-landing-{{ section.id }}__contact-input"
              id="VampireContact-email-{{ section.id }}"
              name="contact[email]"
              autocomplete="email"
              autocapitalize="off"
              spellcheck="false"
              placeholder="{{ 'templates.contact.form.email' | t }}*"
              value="{% if form.email %}{{ form.email }}{% elsif customer %}{{ customer.email }}{% endif %}"
              aria-required="true"
              {% if form.errors contains 'email' %}
                aria-invalid="true"
              {% endif %}
              required
            >
          </div>

          <div class="vampire-landing-{{ section.id }}__contact-field">
            <label class="visually-hidden" for="VampireContact-body-{{ section.id }}">
              {{- 'templates.contact.form.comment' | t -}}
            </label>
            <textarea
              class="vampire-landing-{{ section.id }}__contact-textarea"
              id="VampireContact-body-{{ section.id }}"
              name="contact[{{ 'templates.contact.form.comment' | t }}]"
              placeholder="{{ 'templates.contact.form.comment' | t }}*"
              rows="5"
              required
            >
              {{- form.body -}}
            </textarea>
          </div>

          <div class="vampire-landing-{{ section.id }}__contact-legal">
            <label class="vampire-landing-{{ section.id }}__contact-legal-label">
              <input
                class="vampire-landing-{{ section.id }}__contact-checkbox"
                type="checkbox"
                name="contact[Legal terms accepted]"
                value="yes"
                required
              >
              <span class="vampire-landing-{{ section.id }}__contact-legal-copy">
                <span class="vampire-landing-{{ section.id }}__contact-legal-preface">I accept</span>
                {%- if vampire_terms_url != blank -%}
                  <a
                    href="{{ vampire_terms_url }}"
                    class="vampire-landing-{{ section.id }}__contact-legal-link"
                    data-vampire-contact-legal-about
                    >legal terms</a
                  >
                {%- else -%}
                  <button
                    type="button"
                    class="vampire-landing-{{ section.id }}__contact-legal-link"
                    data-vampire-contact-legal-about
                  >
                    legal terms
                  </button>
                {%- endif -%}
              </span>
            </label>
          </div>

          <button type="submit" class="vampire-landing-{{ section.id }}__contact-send">send</button>
        </div>
      {%- endform -%}
    </aside>
    <button
      type="button"
      class="vampire-landing-{{ section.id }}__contact-scrim"
      aria-label="{{ 'accessibility.close' | t }}"
      data-vampire-close-contact
    ></button>
  </div>

  <div
    id="vampire-about-dialog-{{ section.id }}"
    class="vampire-landing-{{ section.id }}__about-root"
    role="dialog"
    aria-modal="true"
    aria-hidden="true"
    aria-labelledby="vampire-about-title-{{ section.id }}"
  >
    <button
      type="button"
      class="vampire-landing-{{ section.id }}__about-scrim"
      aria-label="{{ 'accessibility.close' | t }}"
      data-vampire-close-about
    ></button>
    <aside class="vampire-landing-{{ section.id }}__about-panel">
      <div class="vampire-landing-{{ section.id }}__about-header">
        <button
          type="button"
          class="vampire-landing-{{ section.id }}__about-close"
          data-vampire-close-about
        >
          [CLOSE]
        </button>
        <h2 class="vampire-landing-{{ section.id }}__about-title" id="vampire-about-title-{{ section.id }}">about</h2>
      </div>
      <div class="vampire-landing-{{ section.id }}__about-mid">
        <div class="vampire-landing-{{ section.id }}__about-copy" lang="en">
          <p>
            Hoopers Soul is more than clothing it&rsquo;s a mindset built on dedication, resilience, and the courage to step into the unknown.
          </p>
          <p>
            The brand was created for those who carry a vision in their head 24/7. The ones who can&rsquo;t ignore what they want to become, even when reality doesn&rsquo;t match it yet. It&rsquo;s inspired by that moment when you realize you might not be ready, but you move forward anyway, because not trying would feel worse.
          </p>
          <p>
            Rooted in basketball culture but extending far beyond it, Hoopers Soul represents a lifestyle of discipline, self-belief, and constant growth. It&rsquo;s about showing up every day, embracing discomfort, and trusting the process without needing to see the final outcome.
          </p>
          <p>
            We don&rsquo;t focus on the winning moment we focus on the work, the doubt, the small progress, and the decision to keep going.
          </p>
          <p>
            Wearing Hoopers Soul means choosing to step into the unknown, driven by a vision you refuse to let go of.
          </p>
        </div>
      </div>
      <div class="vampire-landing-{{ section.id }}__about-footer">
        <span class="vampire-landing-{{ section.id }}__about-legal-title">legal</span>
      </div>
    </aside>
  </div>
</section>

<script>
  (function () {
    const sectionId = '{{ section.id }}';
    const sectionEl = document.getElementById('shopify-section-' + sectionId);

    const countdownEl = document.getElementById('vampire-countdown-' + sectionId);
    if (countdownEl) {
      // 22 May 2026, 10:00 AM Barcelona time (CEST, UTC+2)
      const targetTime = Date.UTC(2026, 4, 22, 8, 0, 0);

      const updateCountdown = () => {
        const now = Date.now();
        const diff = targetTime - now;

        if (diff <= 0) {
          countdownEl.textContent = 'LIVE NOW';
          return;
        }

        const totalSeconds = Math.floor(diff / 1000);
        const hoursRemaining = Math.floor(totalSeconds / 3600);
        const minutesRemaining = Math.floor((totalSeconds % 3600) / 60);
        const secondsRemaining = totalSeconds % 60;
        countdownEl.textContent =
          hoursRemaining +
          'H ' +
          String(minutesRemaining).padStart(2, '0') +
          'M ' +
          String(secondsRemaining).padStart(2, '0') +
          'S';
      };

      updateCountdown();
      setInterval(updateCountdown, 1000);
    }

    if (sectionEl) {
      sectionEl.querySelectorAll('.vampire-landing-' + sectionId + '__video-bg video').forEach(function (v) {
        v.setAttribute('playsinline', '');
        v.setAttribute('data-pause-all-media-exempt', '');
        v.muted = true;
        v.loop = true;
        v.addEventListener('ended', function () {
          v.currentTime = 0;
          var again = v.play();
          if (again && again.catch) again.catch(function () {});
        });
        var playTry = v.play();
        if (playTry && playTry.catch) playTry.catch(function () {});
      });
    }

    if (!sectionEl) return;

    const root = document.getElementById('vampire-contact-dialog-' + sectionId);
    const trigger = document.getElementById('vampire-contact-trigger-' + sectionId);
    const aboutRoot = document.getElementById('vampire-about-dialog-' + sectionId);
    const aboutTrigger = document.getElementById('vampire-about-trigger-' + sectionId);
    function updateBodyOverflow() {
      var contactOpen = root && root.classList.contains('is-open');
      var aboutOpen = aboutRoot && aboutRoot.classList.contains('is-open');
      document.documentElement.style.overflow = contactOpen || aboutOpen ? 'hidden' : '';
      if (sectionEl) {
        sectionEl.classList.toggle('vampire-landing-about-drawer-open', Boolean(aboutOpen));
      }
    }

    function openDrawer() {
      if (!root) return;
      if (aboutRoot) {
        aboutRoot.classList.remove('is-open');
        aboutRoot.setAttribute('aria-hidden', 'true');
        if (aboutTrigger) aboutTrigger.setAttribute('aria-expanded', 'false');
      }
      root.classList.add('is-open');
      root.setAttribute('aria-hidden', 'false');
      if (trigger) trigger.setAttribute('aria-expanded', 'true');
      updateBodyOverflow();
      var toFocus = root.querySelector('.vampire-landing-' + sectionId + '__contact-close');
      if (toFocus) toFocus.focus({ preventScroll: true });
    }

    function closeDrawer() {
      if (!root) return;
      root.classList.remove('is-open');
      root.setAttribute('aria-hidden', 'true');
      if (trigger) trigger.setAttribute('aria-expanded', 'false');
      updateBodyOverflow();
      if (trigger) trigger.focus({ preventScroll: true });
    }

    function openAboutDrawer() {
      if (!aboutRoot) return;
      if (root) {
        root.classList.remove('is-open');
        root.setAttribute('aria-hidden', 'true');
        if (trigger) trigger.setAttribute('aria-expanded', 'false');
      }
      aboutRoot.classList.add('is-open');
      aboutRoot.setAttribute('aria-hidden', 'false');
      if (aboutTrigger) aboutTrigger.setAttribute('aria-expanded', 'true');
      updateBodyOverflow();
      var toFocusAbout = aboutRoot.querySelector('.vampire-landing-' + sectionId + '__about-close');
      if (toFocusAbout) toFocusAbout.focus({ preventScroll: true });
    }

    function closeAboutDrawer() {
      if (!aboutRoot) return;
      aboutRoot.classList.remove('is-open');
      aboutRoot.setAttribute('aria-hidden', 'true');
      if (aboutTrigger) aboutTrigger.setAttribute('aria-expanded', 'false');
      updateBodyOverflow();
      if (aboutTrigger) aboutTrigger.focus({ preventScroll: true });
    }

    if (trigger && root) {
      trigger.addEventListener('click', function () {
        openDrawer();
      });
    }

    if (aboutTrigger && aboutRoot) {
      aboutTrigger.addEventListener('click', function () {
        openAboutDrawer();
      });
    }

    if (root) {
      root.addEventListener('click', function (e) {
        if (!root.classList.contains('is-open')) return;
        if (e.target.closest('[data-vampire-contact-legal-about]')) {
          e.preventDefault();
          e.stopPropagation();
          openAboutDrawer();
          return;
        }
        if (e.target.closest('[data-vampire-close-contact]')) {
          closeDrawer();
        }
      });
    }

    if (aboutRoot) {
      aboutRoot.addEventListener('click', function (e) {
        if (!aboutRoot.classList.contains('is-open')) return;
        if (e.target.closest('[data-vampire-close-about]')) {
          closeAboutDrawer();
        }
      });
    }

    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Escape') return;
      if (root && root.classList.contains('is-open')) {
        closeDrawer();
      } else if (aboutRoot && aboutRoot.classList.contains('is-open')) {
        closeAboutDrawer();
      }
    });

    var params = new URLSearchParams(window.location.search);
    if (params.get('contact_posted') === 'true') {
      var successEl = document.getElementById('vampire-contact-success-' + sectionId);
      var fieldsEl = document.getElementById('vampire-contact-fields-' + sectionId);
      if (successEl) successEl.hidden = false;
      if (fieldsEl) fieldsEl.hidden = true;
      openDrawer();
      if (successEl) successEl.focus({ preventScroll: true });
    }

    var okBanner = document.getElementById('vampire-contact-success-' + sectionId);
    var fieldWrap = document.getElementById('vampire-contact-fields-' + sectionId);
    if (okBanner && fieldWrap && !okBanner.hidden && fieldWrap.hidden) {
      openDrawer();
      okBanner.focus({ preventScroll: true });
    }
  })();
</script>

{% schema %}
{
  "name": "Landing vampire inspired",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Background video"
    },
    {
      "type": "video",
      "id": "background_video",
      "label": "Video (recommended)",
      "info": "Upload in Content → Files, then choose here. If empty, use “Theme asset filename” below."
    },
    {
      "type": "text",
      "id": "background_video_asset",
      "label": "Theme asset filename (fallback)",
      "default": "landing-background.mov",
      "info": "Must be uploaded to Assets (e.g. landing-background.mp4). .mov works best in Safari; for Chrome desktop, export H.264 .mp4 with the same name."
    },
    {
      "type": "range",
      "id": "background_overlay_opacity",
      "label": "Dark overlay over video",
      "min": 0,
      "max": 85,
      "step": 5,
      "unit": "%",
      "default": 40,
      "info": "Keeps headline text readable over bright footage."
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "aria_label",
      "label": "Accessibility label",
      "default": "Landing hero"
    },
    {
      "type": "text",
      "id": "brand_wordmark",
      "label": "Top center wordmark",
      "default": "VAMPIRE"
    },
    {
      "type": "text",
      "id": "top_right_label",
      "label": "Top right label",
      "default": "contact about",
      "info": "First word opens the contact panel; second word opens the blue about panel (only one open at a time). Any further words display after those buttons."
    },
    {
      "type": "url",
      "id": "legal_terms_url",
      "label": "Legal terms link (optional)",
      "info": "Linked from “legal terms” in the contact panel. If empty, your store’s Terms of service policy URL is used when set in Shopify admin."
    },
    {
      "type": "text",
      "id": "phone_line",
      "label": "Phone line",
      "default": "Tel. (+34 93 301 00 90)"
    },
    {
      "type": "text",
      "id": "center_text",
      "label": "Center text",
      "default": "for comissions"
    },
    {
      "type": "text",
      "id": "left_bottom_label",
      "label": "Bottom left text",
      "default": "studio"
    },
    {
      "type": "text",
      "id": "right_bottom_label",
      "label": "Bottom right text",
      "default": "films"
    },
    {
      "type": "textarea",
      "id": "small_copy",
      "label": "Small repeated copy",
      "default": "Everything we do aims at building bridges between youth culture and music, navigating through talent, media, and brands."
    },
    {
      "type": "url",
      "id": "shop_page_url",
      "label": "Shop page URL (optional)",
      "info": "Overrides automatic shop link. If empty, uses the Page with handle shop when it exists, otherwise /pages/shop. Create a Page titled Shop (handle shop); the theme shows the custom shop layout even if that Page still uses the default Page template."
    }
  ],
  "presets": [
    {
      "name": "Landing vampire inspired"
    }
  ]
}
{% endschema %}
